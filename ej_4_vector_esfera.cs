using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ej_4_vector_esfera : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Debug.Log("Vector de posición de esfera: " + GetComponent<Transform>().position);
        Debug.Log("Vector de posición de esfera: " + transform.position);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
